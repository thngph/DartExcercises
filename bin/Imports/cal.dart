import 'dart:math';

/*
Create two functions that print the square and the logarithm of a given
number using math library.
*/

double m_square(double x) {
  return x * x;
}

double m_log(double x) {
  return log(x);
}
